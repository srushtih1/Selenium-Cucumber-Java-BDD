package com.qa.util;

public class TestUtil {
	
	//it is static so that we can call it using the class name (no need for object creation or inherit)
	static int PAGE_LOAD_TIMEOUT = 20;
	static int IMPLICIT_WAIT = 30;
}
